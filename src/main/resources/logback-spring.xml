<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>
    <springProperty scope="context" name="application" source="spring.application.name" defaultValue="micro-service"/>
    <springProperty scope="context" name="port" source="server.port" defaultValue="0000"/>
    <!--    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">-->
    <!--        <encoder>-->
    <!--            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>-->
    <!--        </encoder>-->
    <!--    </appender>-->

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>/opt/middleware/logs/${application}/${application}-${port}.log</file>
        <append>true</append>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- daily rollover -->
            <fileNamePattern>/opt/middleware/logs/${application}/${application}-${port}.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy
                    class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <!-- or whenever the file size reaches 50MB -->
                <maxFileSize>50MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <!-- keep 90 files worth of history -->
            <!--            <maxHistory>90</maxHistory>-->
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!--    <springProperty scope="context" name="emailHost" source="spring.mail.host" defaultValue="0000"/>-->

    <!--    <appender name="EMAIL" class="ch.qos.logback.classic.net.SMTPAppender">-->
    <!--        <smtpHost>${emailHost}</smtpHost>-->
    <!--        <to>EMAIL-DESTINATION</to>-->
    <!--        <to>ANOTHER_EMAIL_DESTINATION</to> &lt;!&ndash; additional destinations are possible &ndash;&gt;-->
    <!--        <from>SENDER-EMAIL</from>-->
    <!--        <subject>TESTING: %logger{20} - %m</subject>-->
    <!--        <layout class="ch.qos.logback.classic.PatternLayout">-->
    <!--            <pattern>%date %-5level %logger{35} - %message%n</pattern>-->
    <!--        </layout>-->
    <!--    </appender>-->
    <root level="info">
        <appender-ref ref="FILE" />
        <appender-ref ref="CONSOLE"  />
    </root>
    <logger name="com.msejemplo01.msfirmadigital" level="INFO"/>
    <logger name="com.msejemplo01.msfirmadigital" level="DEBUG"/>
</configuration>
